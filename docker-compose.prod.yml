version: '3'

services:

  nginx:
    volumes:
      - certificates:/etc/letsencrypt
      - ./docker/nginx/conf/nginx.prod.conf:/etc/nginx/nginx.conf
    depends_on:
      - nodejs
      - certbot

  certbot:
    image: certbot/certbot
    volumes:
      - certificates:/etc/letsencrypt
      - ./frontend/public:/www
    entrypoint: sh -c 'certbot certonly --dry-run --authenticator webroot -d listodo.ru --webroot-path /www'

volumes:
  certificates:
